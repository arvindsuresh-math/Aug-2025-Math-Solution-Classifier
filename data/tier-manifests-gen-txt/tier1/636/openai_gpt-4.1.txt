{
  "function_code": "def solve():\n    \"\"\"Index: 636.\n    Returns: the total minutes Tina needs to clean the remaining keys and finish her assignment.\n    \"\"\"\n    # L1\n    keys_left = 14 # there are 14 left to clean\n    minutes_per_key = 3 # 3 minutes to clean one\n    cleaning_time = keys_left * minutes_per_key\n\n    # L2\n    assignment_time = 10 # assignment will only take 10 minutes\n    total_time = cleaning_time + assignment_time\n\n    # FA\n    answer = total_time\n    return answer",
  "logical_steps": [
    {
      "line_number": "L1",
      "question_inputs": ["keys_left", "minutes_per_key"],
      "WK_inputs": [],
      "output_variable": "cleaning_time",
      "solution_line_template": "Tina has already cleaned one key so she has {keys_left} left which take {minutes_per_key} minutes each to clean, {keys_left} x {minutes_per_key} = <<{keys_left}*{minutes_per_key}={cleaning_time}>>{cleaning_time} minutes to clean all the keyboard keys."
    },
    {
      "line_number": "L2",
      "question_inputs": ["assignment_time"],
      "WK_inputs": [],
      "output_variable": "total_time",
      "solution_line_template": "Her assignment will take {assignment_time} minutes to complete, so she needs {cleaning_time} minutes + {assignment_time} minutes = {total_time} minutes total before dinner."
    }
  ]
}