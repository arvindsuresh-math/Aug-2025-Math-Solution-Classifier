{
  "function_code": "def solve():\n    \"\"\"Index: 82.\n    Returns: Irene's total income for the week including overtime.\n    \"\"\"\n    # L1\n    hours_worked = 50 # worked 50 hours last week\n    regular_hours = 40 # works for 40 hours a week\n    overtime_hours = hours_worked - regular_hours\n\n    # L2\n    overtime_rate = 20 # gets an extra $20 for every hour of overtime\n    overtime_pay = overtime_hours * overtime_rate\n\n    # L3\n    base_pay = 500 # earns $500 if she works for 40 hours a week\n    total_income = base_pay + overtime_pay\n\n    # FA\n    answer = total_income\n    return answer",
  "logical_steps": [
    {
      "line_number": "L1",
      "question_inputs": ["hours_worked", "regular_hours"],
      "WK_inputs": [],
      "output_variable": "overtime_hours",
      "solution_line_template": "If Irene worked {hours_worked} hours last week, the total number of hours counting as overtime is {hours_worked}-{regular_hours} = <<{hours_worked}-{regular_hours}={overtime_hours}>>{overtime_hours} hours."
    },
    {
      "line_number": "L2",
      "question_inputs": ["overtime_rate"],
      "WK_inputs": [],
      "output_variable": "overtime_pay",
      "solution_line_template": "Since she's given ${overtime_rate} for every hour of overtime, she earned {overtime_hours}*${overtime_rate} = $<<{overtime_hours}*{overtime_rate}={overtime_pay}>>{overtime_pay} in overtime."
    },
    {
      "line_number": "L3",
      "question_inputs": ["base_pay"],
      "WK_inputs": [],
      "output_variable": "total_income",
      "solution_line_template": "Her total income, including the overtime, is ${base_pay} + ${overtime_pay}= $<<{base_pay}+{overtime_pay}={total_income}>>{total_income}"
    }
  ]
}