{
  "function_code": "def solve():\n    \"\"\"Index: 1687.\n    Returns: how many more kilograms can still be loaded into the truck.\n    \"\"\"\n    # L1\n    num_bags = 100 # 100 bags of lemons\n    mass_per_bag = 8 # mass of 8 kilograms per bag\n    total_weight = num_bags * mass_per_bag\n\n    # L2\n    max_truck_load = 900 # load on a truck may not be more than 900 kilograms\n    remaining_capacity = max_truck_load - total_weight\n\n    # FA\n    answer = remaining_capacity\n    return answer",
  "logical_steps": [
    {
      "line_number": "L1",
      "question_inputs": ["num_bags", "mass_per_bag"],
      "WK_inputs": [],
      "output_variable": "total_weight",
      "solution_line_template": "The total weight of {num_bags} bags of lemons is {num_bags} x {mass_per_bag} = <<{num_bags}*{mass_per_bag}={total_weight}>>{total_weight} kilograms."
    },
    {
      "line_number": "L2",
      "question_inputs": ["max_truck_load"],
      "WK_inputs": [],
      "output_variable": "remaining_capacity",
      "solution_line_template": "So, {max_truck_load} - {total_weight} = <<{max_truck_load}-{total_weight}={remaining_capacity}>>{remaining_capacity} kilograms can still be loaded into the truck."
    }
  ]
}