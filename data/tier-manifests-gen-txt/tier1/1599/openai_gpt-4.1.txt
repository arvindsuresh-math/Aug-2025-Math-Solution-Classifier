{
  "function_code": "def solve():\n    \"\"\"Index: 1599.\n    Returns: the amount of money Natalie has left after shopping.\n    \"\"\"\n    # L1\n    jumper_cost = 9 # jumper for $9\n    tshirt_cost = 4 # T-shirt for $4\n    heels_cost = 5 # pair of heels for $5\n    total_cost = jumper_cost + tshirt_cost + heels_cost\n\n    # L2\n    natalie_money = 26 # Natalie has $26\n    money_left = natalie_money - total_cost\n\n    # FA\n    answer = money_left\n    return answer",
  "logical_steps": [
    {
      "line_number": "L1",
      "question_inputs": ["jumper_cost", "tshirt_cost", "heels_cost"],
      "WK_inputs": [],
      "output_variable": "total_cost",
      "solution_line_template": "The total cost of the clothes is ${jumper_cost} + ${tshirt_cost} + ${heels_cost} = $<<{jumper_cost}+{tshirt_cost}+{heels_cost}={total_cost}>>{total_cost}."
    },
    {
      "line_number": "L2",
      "question_inputs": ["natalie_money"],
      "WK_inputs": [],
      "output_variable": "money_left",
      "solution_line_template": "Natalie has ${natalie_money} − ${total_cost} = ${money_left} left."
    }
  ]
}