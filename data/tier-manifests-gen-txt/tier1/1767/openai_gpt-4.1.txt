{
  "function_code": "def solve():\n    \"\"\"Index: 1767.\n    Returns: the number of chives Juvy will plant.\n    \"\"\"\n    # L1\n    parsley_rows = 3 # parsley in the first 3 rows\n    rosemary_rows = 2 # rosemary in the last two rows\n    non_chive_rows = parsley_rows + rosemary_rows\n\n    # L2\n    total_rows = 20 # garden has 20 rows\n    chive_rows = total_rows - non_chive_rows\n\n    # L3\n    plants_per_row = 10 # 10 plants in each row\n    chives_planted = chive_rows * plants_per_row\n\n    # FA\n    answer = chives_planted\n    return answer",
  "logical_steps": [
    {
      "line_number": "L1",
      "question_inputs": ["parsley_rows", "rosemary_rows"],
      "WK_inputs": [],
      "output_variable": "non_chive_rows",
      "solution_line_template": "Juvy planted {parsley_rows} + {rosemary_rows} = <<{parsley_rows}+{rosemary_rows}={non_chive_rows}>>{non_chive_rows} rows with parsley and rosemary."
    },
    {
      "line_number": "L2",
      "question_inputs": ["total_rows"],
      "WK_inputs": [],
      "output_variable": "chive_rows",
      "solution_line_template": "So, {total_rows} - {non_chive_rows} = <<{total_rows}-{non_chive_rows}={chive_rows}>>{chive_rows} rows will be planted with chives."
    },
    {
      "line_number": "L3",
      "question_inputs": ["plants_per_row"],
      "WK_inputs": [],
      "output_variable": "chives_planted",
      "solution_line_template": "Therefore, {chive_rows} x {plants_per_row} = <<{chive_rows}*{plants_per_row}={chives_planted}>>{chives_planted} chives will be planted."
    }
  ]
}