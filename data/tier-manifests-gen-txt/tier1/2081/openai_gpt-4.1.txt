{
  "function_code": "def solve():\n    \"\"\"Index: 2081.\n    Returns: the total number of days Cassidy is grounded for.\n    \"\"\"\n    # L1\n    num_grades_below_B = 4 # four grades below a B\n    extra_days_per_grade = 3 # 3 extra days for each grade below a B\n    extra_days = num_grades_below_B * extra_days_per_grade\n\n    # L2\n    base_days = 14 # grounded for 14 days for lying about her report card\n    total_days = extra_days + base_days\n\n    # FA\n    answer = total_days\n    return answer",
  "logical_steps": [
    {
      "line_number": "L1",
      "question_inputs": ["num_grades_below_B", "extra_days_per_grade"],
      "WK_inputs": [],
      "output_variable": "extra_days",
      "solution_line_template": "First find how many days Cassidy is grounded for the low grades: {num_grades_below_B} grades * {extra_days_per_grade} days/grade = <<{num_grades_below_B}*{extra_days_per_grade}={extra_days}>>{extra_days} days"
    },
    {
      "line_number": "L2",
      "question_inputs": ["base_days"],
      "WK_inputs": [],
      "output_variable": "total_days",
      "solution_line_template": "Then add that time to the {base_days} days she's grounded for lying to find the total grounding length: {extra_days} days + {base_days} days = <<{extra_days}+{base_days}={total_days}>>{total_days} days"
    }
  ]
}