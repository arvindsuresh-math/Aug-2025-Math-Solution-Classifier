{
  "function_code": "def solve():\n    \"\"\"Index: 1753.\n    Returns: the total cost for enough sugar substitute to last 90 days.\n    \"\"\"\n    # L1\n    packets_per_cup = 1 # 1 packet per cup of coffee\n    cups_per_day = 2 # 2 coffees a day\n    packets_per_day = packets_per_cup * cups_per_day\n\n    # L2\n    num_days = 90 # 90 days\n    total_packets = num_days * packets_per_day\n\n    # L3\n    packets_per_box = 30 # packets come 30 to a box\n    num_boxes = total_packets / packets_per_box\n\n    # L4\n    cost_per_box = 4 # $4.00 a box\n    total_cost = cost_per_box * num_boxes\n\n    # FA\n    answer = total_cost\n    return answer",
  "logical_steps": [
    {
      "line_number": "L1",
      "question_inputs": ["packets_per_cup", "cups_per_day"],
      "WK_inputs": [],
      "output_variable": "packets_per_day",
      "solution_line_template": "He uses {packets_per_cup} packet per cup of coffee and he has {cups_per_day} cups a day so that's {packets_per_cup}*{cups_per_day} = <<{packets_per_cup}*{cups_per_day}={packets_per_day}>>{packets_per_day} packets"
    },
    {
      "line_number": "L2",
      "question_inputs": ["num_days"],
      "WK_inputs": [],
      "output_variable": "total_packets",
      "solution_line_template": "He wants enough to last him {num_days} days and he uses {packets_per_day} packets a day so that's {num_days}*{packets_per_day} = <<{num_days}*{packets_per_day}={total_packets}>>{total_packets} packets"
    },
    {
      "line_number": "L3",
      "question_inputs": ["packets_per_box"],
      "WK_inputs": [],
      "output_variable": "num_boxes",
      "solution_line_template": "The packets come {packets_per_box} to a box and he needs {total_packets} packets so that's {total_packets}/{packets_per_box} = <<{total_packets}/{packets_per_box}={num_boxes}>>{num_boxes} boxes of sugar substitute"
    },
    {
      "line_number": "L4",
      "question_inputs": ["cost_per_box"],
      "WK_inputs": [],
      "output_variable": "total_cost",
      "solution_line_template": "Each box costs ${cost_per_box}.00 and he needs {num_boxes} boxes so that's {cost_per_box}*{num_boxes} = $<<{cost_per_box}*{num_boxes}={total_cost}>>{total_cost}"
    }
  ]
}
