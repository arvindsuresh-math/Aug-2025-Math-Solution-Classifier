{
  "function_code": "def solve():\n    \"\"\"Index: 2555.\n    Returns: the weight of the moon in tons.\n    \"\"\"\n    # L1\n    total_percent = 100 # WK\n    iron_percent = 50 # 50% iron\n    carbon_percent = 20 # 20% carbon\n    other_percent = total_percent - iron_percent - carbon_percent\n\n    # L2\n    mars_other_tons = 150 # Mars is 150 tons of other elements\n    other_percent_decimal = 0.3 # 30% as decimal\n    mars_weight = mars_other_tons / other_percent_decimal\n\n    # L3\n    mars_to_moon_ratio = 2 # Mars weighs twice as much as the moon\n    moon_weight = mars_weight / mars_to_moon_ratio\n\n    # FA\n    answer = moon_weight\n    return answer",
  "logical_steps": [
    {
      "line_number": "L1",
      "question_inputs": ["iron_percent", "carbon_percent"],
      "WK_inputs": ["total_percent"],
      "output_variable": "other_percent",
      "solution_line_template": "30% of Mars is made up of other elements because {total_percent} - {iron_percent} - {carbon_percent} = <<{total_percent}-{iron_percent}-{carbon_percent}={other_percent}>>{other_percent}"
    },
    {
      "line_number": "L2",
      "question_inputs": ["mars_other_tons"],
      "WK_inputs": ["other_percent_decimal"],
      "output_variable": "mars_weight",
      "solution_line_template": "Mars weighs {mars_weight} tons because {mars_other_tons} / {other_percent_decimal} = <<{mars_other_tons}/{other_percent_decimal}={mars_weight}>>{mars_weight}"
    },
    {
      "line_number": "L3",
      "question_inputs": [],
      "WK_inputs": ["mars_to_moon_ratio"],
      "output_variable": "moon_weight",
      "solution_line_template": "The moon weighs {moon_weight} tons because {mars_weight} / {mars_to_moon_ratio} = <<{mars_weight}/{mars_to_moon_ratio}={moon_weight}>>{moon_weight}"
    }
  ]
}
