{
  "function_code": "def solve():\n    \"\"\"Index: 926.\n    Returns: the total amount Bailey will spend on towel sets after the discount.\n    \"\"\"\n    # L1\n    num_guest_sets = 2 # 2 new sets of towels for the guest bathroom\n    guest_set_price = 40.00 # set of towels for the guest bathroom are $40.00 each\n    guest_total = num_guest_sets * guest_set_price\n\n    # L2\n    num_master_sets = 4 # 4 new sets for her master bathroom\n    master_set_price = 50.00 # master bathroom set is $50.00 each\n    master_total = num_master_sets * master_set_price\n\n    # L3\n    towels_total = guest_total + master_total\n\n    # L4\n    discount_percent_decimal = 0.20 # 20% off\n    discount_amount = towels_total * discount_percent_decimal\n\n    # L5\n    final_total = towels_total - discount_amount\n\n    # FA\n    answer = final_total\n    return answer",
  "logical_steps": [
    {
      "line_number": "L1",
      "question_inputs": ["num_guest_sets", "guest_set_price"],
      "WK_inputs": [],
      "output_variable": "guest_total",
      "solution_line_template": "She needs {num_guest_sets} sets of guest towels that are ${guest_set_price} each so that's {num_guest_sets}*{guest_set_price} = $<<{num_guest_sets}*{guest_set_price}={guest_total}>>{guest_total}"
    },
    {
      "line_number": "L2",
      "question_inputs": ["num_master_sets", "master_set_price"],
      "WK_inputs": [],
      "output_variable": "master_total",
      "solution_line_template": "She needs {num_master_sets} sets of master bath towels that are ${master_set_price} each so that's {num_master_sets}*{master_set_price} = $<<{num_master_sets}*{master_set_price}={master_total}>>{master_total}"
    },
    {
      "line_number": "L3",
      "question_inputs": [],
      "WK_inputs": [],
      "output_variable": "towels_total",
      "solution_line_template": "Together, the towels will cost {guest_total}+{master_total} = $<<{guest_total}+{master_total}={towels_total}>>{towels_total}"
    },
    {
      "line_number": "L4",
      "question_inputs": ["discount_percent_decimal"],
      "WK_inputs": [],
      "output_variable": "discount_amount",
      "solution_line_template": "The store is offering 20% off of her ${towels_total} towels so that's {towels_total}*{discount_percent_decimal} = $<<{towels_total}*{discount_percent_decimal}={discount_amount}>>{discount_amount} discount"
    },
    {
      "line_number": "L5",
      "question_inputs": [],
      "WK_inputs": [],
      "output_variable": "final_total",
      "solution_line_template": "Her ${towels_total} towels have a ${discount_amount} discount making them {towels_total}-{discount_amount} =$<<{towels_total}-{discount_amount}={final_total}>>{final_total}"
    }
  ]
}
