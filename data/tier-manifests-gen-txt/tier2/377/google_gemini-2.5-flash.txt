{
  "function_code": "def solve():\n    \"\"\"Index: 377.\n    Returns: the combined length of the CDs.\n    \"\"\"\n    # L1\n    short_cd_length = 1.5 # Two of them are 1.5 hours each\n    long_cd_length = short_cd_length * 2\n\n    # L2\n    num_shorter_cds = 2 # Two of them are 1.5 hours each\n    combined_short_cd_length = short_cd_length * num_shorter_cds\n\n    # L3\n    total_length = long_cd_length + combined_short_cd_length\n\n    # FA\n    answer = total_length\n    return answer",
  "logical_steps": [
    {
      "WK_inputs": [],
      "line_number": "L1",
      "output_variable": "long_cd_length",
      "question_inputs": ["short_cd_length"],
      "solution_line_template": "The long CD is {short_cd_length}*2=<<{short_cd_length}*2={long_cd_length}>>{long_cd_length} hours"
    },
    {
      "WK_inputs": [],
      "line_number": "L2",
      "output_variable": "combined_short_cd_length",
      "question_inputs": ["num_shorter_cds"],
      "solution_line_template": "The two shorter CDs have a combined length of {short_cd_length}*{num_shorter_cds}=<<{short_cd_length}*{num_shorter_cds}={combined_short_cd_length}>>{combined_short_cd_length} hours"
    },
    {
      "WK_inputs": [],
      "line_number": "L3",
      "output_variable": "total_length",
      "question_inputs": [],
      "solution_line_template": "So in total the CDs had a length of {long_cd_length}+{combined_short_cd_length}= <<{long_cd_length}+{combined_short_cd_length}={total_length}>>{total_length} hours"
    }
  ]
}