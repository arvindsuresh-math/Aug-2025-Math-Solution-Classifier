{
  "function_code": "def solve():\n    \"\"\"Index: 1441.\n    Returns: the total number of calories James burns per week.\n    \"\"\"\n    # L1\n    hours_per_class = 1.5 # works out for 1.5 hours each class\n    minutes_per_hour = 60 # WK\n    minutes_per_class = hours_per_class * minutes_per_hour\n\n    # L2\n    calories_per_minute = 7 # burns 7 calories per minute\n    calories_per_class = minutes_per_class * calories_per_minute\n\n    # L3\n    classes_per_week = 3 # 3 times a week\n    total_calories_per_week = calories_per_class * classes_per_week\n\n    # FA\n    answer = total_calories_per_week\n    return answer",
  "logical_steps": [
    {
      "line_number": "L1",
      "question_inputs": ["hours_per_class"],
      "WK_inputs": ["minutes_per_hour"],
      "output_variable": "minutes_per_class",
      "solution_line_template": "He works out for {hours_per_class}*{minutes_per_hour}=<<{hours_per_class}*{minutes_per_hour}={minutes_per_class}>>{minutes_per_class} minutes per class"
    },
    {
      "line_number": "L2",
      "question_inputs": ["calories_per_minute"],
      "WK_inputs": [],
      "output_variable": "calories_per_class",
      "solution_line_template": "So he burns {minutes_per_class}*{calories_per_minute}=<<{minutes_per_class}*{calories_per_minute}={calories_per_class}>>{calories_per_class} calories per class"
    },
    {
      "line_number": "L3",
      "question_inputs": ["classes_per_week"],
      "WK_inputs": [],
      "output_variable": "total_calories_per_week",
      "solution_line_template": "That means he burns {calories_per_class}*{classes_per_week}=<<{calories_per_class}*{classes_per_week}={total_calories_per_week}>>{total_calories_per_week} calories per week"
    }
  ]
}
