{
  "function_code": "def solve():\n    \"\"\"Index: 2905.\n    Returns: the total length of the rubber, pen, and pencil altogether.\n    \"\"\"\n    # L1\n    pencil_length = 12 # pencil is 12 centimeters long\n    pen_shorter_than_pencil = 2 # pen is shorter than the pencil by 2 centimeters\n    pen_length = pencil_length - pen_shorter_than_pencil\n\n    # L2\n    pen_longer_than_rubber = 3 # pen is longer than the rubber by 3 centimeters\n    rubber_length = pen_length - pen_longer_than_rubber\n\n    # L3\n    total_length = rubber_length + pen_length + pencil_length\n\n    # FA\n    answer = total_length\n    return answer",
  "logical_steps": [
    {
      "line_number": "L1",
      "question_inputs": ["pencil_length", "pen_shorter_than_pencil"],
      "WK_inputs": [],
      "output_variable": "pen_length",
      "solution_line_template": "The pen is shorter than the pencil by {pen_shorter_than_pencil} centimeters, so it's {pencil_length} - {pen_shorter_than_pencil} = <<{pencil_length}-{pen_shorter_than_pencil}={pen_length}>>{pen_length} centimeters long."
    },
    {
      "line_number": "L2",
      "question_inputs": ["pen_longer_than_rubber"],
      "WK_inputs": [],
      "output_variable": "rubber_length",
      "solution_line_template": "That means the rubber is {pen_length} - {pen_longer_than_rubber} = <<{pen_length}-{pen_longer_than_rubber}={rubber_length}>>{rubber_length} centimeters long"
    },
    {
      "line_number": "L3",
      "question_inputs": [],
      "WK_inputs": [],
      "output_variable": "total_length",
      "solution_line_template": "So all accessories are {rubber_length} + {pen_length} + {pencil_length} = <<{rubber_length}+{pen_length}+{pencil_length}={total_length}>>{total_length} centimeters long altogether."
    }
  ]
}