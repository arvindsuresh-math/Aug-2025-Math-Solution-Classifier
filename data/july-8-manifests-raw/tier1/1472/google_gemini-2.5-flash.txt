```json
{
  "function_code": "def solve():\n    \"\"\"Index: 1472.\n    Returns: the amount Shara will still owe her brother.\n    \"\"\"\n    # L1\n    amount_per_month = 10 # returned $10 per month\n    months_returned = 6 # 6 months ago\n    total_returned_so_far = amount_per_month * months_returned\n\n    # L2\n    amount_currently_owed = total_returned_so_far\n\n    # L3\n    months_from_now = 4 # 4 months from now\n    additional_payment = amount_per_month * months_from_now\n\n    # L4\n    remaining_debt = amount_currently_owed - additional_payment\n\n    # FA\n    answer = remaining_debt\n    return answer",
  "logical_steps": [
    {
      "line_number": "L1",
      "question_inputs": ["amount_per_month", "months_returned"],
      "WK_inputs": [],
      "output_variable": "total_returned_so_far",
      "solution_line_template": "For {months_returned} months, Shara was able to return ${amount_per_month} x {months_returned} = $<<{amount_per_month}*{months_returned}={total_returned_so_far}>>{total_returned_so_far} to her brother."
    },
    {
      "line_number": "L2",
      "question_inputs": [],
      "WK_inputs": [],
      "output_variable": "amount_currently_owed",
      "solution_line_template": "Since she already returned half of the money, then this means she currently owes her brother $<<{total_returned_so_far}={amount_currently_owed}>>{amount_currently_owed}."
    },
    {
      "line_number": "L3",
      "question_inputs": ["months_from_now"],
      "WK_inputs": [],
      "output_variable": "additional_payment",
      "solution_line_template": "Four months from now, she will be able to pay an additional ${amount_per_month} x {months_from_now} = $<<{amount_per_month}*{months_from_now}={additional_payment}>>{additional_payment}."
    },
    {
      "line_number": "L4",
      "question_inputs": [],
      "WK_inputs": [],
      "output_variable": "remaining_debt",
      "solution_line_template": "Thus, she will still owe her brother ${amount_currently_owed} - ${additional_payment} = $<<{amount_currently_owed}-{additional_payment}={remaining_debt}>>{remaining_debt} four months from now."
    }
  ]
}
```