```json
{
  "function_code": "def solve():\n    \"\"\"Index: 2442.\n    Returns: how long Morgan can hula hoop.\n    \"\"\"\n    # L1\n    nancy_time = 10 # 10 minutes\n    casey_less_than_nancy = 3 # 3 minutes less than Nancy\n    casey_time = nancy_time - casey_less_than_nancy\n\n    # L2\n    morgan_multiplier = 3 # three times as long as Casey\n    morgan_time = morgan_multiplier * casey_time\n\n    # FA\n    answer = morgan_time\n    return answer",
  "logical_steps": [
    {
      "line_number": "L1",
      "question_inputs": ["nancy_time", "casey_less_than_nancy"],
      "WK_inputs": [],
      "output_variable": "casey_time",
      "solution_line_template": "Casey can hula hoop {casey_less_than_nancy} minutes less than Nancy so she can hula hoop for {nancy_time}-{casey_less_than_nancy} = <<{nancy_time}-{casey_less_than_nancy}={casey_time}>>{casey_time} minutes"
    },
    {
      "line_number": "L2",
      "question_inputs": ["morgan_multiplier"],
      "WK_inputs": [],
      "output_variable": "morgan_time",
      "solution_line_template": "Morgan can hula hoop three times as long as Casey, who can hula hoop for {casey_time} minutes, so she can hula hoop for {morgan_multiplier}*{casey_time} = <<{morgan_multiplier}*{casey_time}={morgan_time}>>{morgan_time} minutes"
    }
  ]
}
```