{
  "function_code": "def solve():\n    \"\"\"Index: 2734.\n    Returns: the total number of leaves on all the plants.\n    \"\"\"\n    # L1\n    basil_pots = 3 # 3 pots of basil\n    basil_leaves_per_pot = 4 # each basil plant has 4 leaves\n    basil_leaves = basil_pots * basil_leaves_per_pot\n\n    # L2\n    rosemary_pots = 9 # 9 pots of rosemary\n    rosemary_leaves_per_pot = 18 # each rosemary plant has 18 leaves\n    rosemary_leaves = rosemary_pots * rosemary_leaves_per_pot\n\n    # L3\n    thyme_pots = 6 # 6 pots of thyme\n    thyme_leaves_per_pot = 30 # each thyme plant has 30 leaves\n    thyme_leaves = thyme_pots * thyme_leaves_per_pot\n\n    # L4\n    total_leaves = basil_leaves + rosemary_leaves + thyme_leaves\n\n    # FA\n    answer = total_leaves\n    return answer",
  "logical_steps": [
    {
      "line_number": "L1",
      "question_inputs": ["basil_pots", "basil_leaves_per_pot"],
      "WK_inputs": [],
      "output_variable": "basil_leaves",
      "solution_line_template": "First find the total number of basil leaves: {basil_pots} pots * {basil_leaves_per_pot} leaves/pot = <<{basil_pots}*{basil_leaves_per_pot}={basil_leaves}>>{basil_leaves} leaves"
    },
    {
      "line_number": "L2",
      "question_inputs": ["rosemary_pots", "rosemary_leaves_per_pot"],
      "WK_inputs": [],
      "output_variable": "rosemary_leaves",
      "solution_line_template": "Then find the total number of rosemary leaves: {rosemary_pots} pots * {rosemary_leaves_per_pot} leaves/pot = <<{rosemary_pots}*{rosemary_leaves_per_pot}={rosemary_leaves}>>{rosemary_leaves} leaves"
    },
    {
      "line_number": "L3",
      "question_inputs": ["thyme_pots", "thyme_leaves_per_pot"],
      "WK_inputs": [],
      "output_variable": "thyme_leaves",
      "solution_line_template": "Then find the total number of thyme leaves: {thyme_pots} pots * {thyme_leaves_per_pot} leaves/pot = <<{thyme_pots}*{thyme_leaves_per_pot}={thyme_leaves}>>{thyme_leaves} leaves"
    },
    {
      "line_number": "L4",
      "question_inputs": [],
      "WK_inputs": [],
      "output_variable": "total_leaves",
      "solution_line_template": "Then add the number of each type of leaf to find the total number of leaves: {basil_leaves} leaves + {rosemary_leaves} leaves + {thyme_leaves} leaves = <<{basil_leaves}+{rosemary_leaves}+{thyme_leaves}={total_leaves}>>{total_leaves} leaves"
    }
  ]
}