```json
{
  "function_code": "def solve():\n    \"\"\"Index: 2385.\n    Returns: the total number of cans Jack can store in each closet.\n    \"\"\"\n    # L1\n    cans_per_row = 12 # 12 cans in one row\n    rows_per_shelf = 4 # 4 rows on one shelf\n    cans_per_shelf = cans_per_row * rows_per_shelf\n\n    # L2\n    shelves_per_closet = 10 # 10 shelves in one closet\n    total_cans_in_closet = cans_per_shelf * shelves_per_closet\n\n    # FA\n    answer = total_cans_in_closet\n    return answer",
  "logical_steps": [
    {
      "line_number": "L1",
      "question_inputs": ["cans_per_row", "rows_per_shelf"],
      "WK_inputs": [],
      "output_variable": "cans_per_shelf",
      "solution_line_template": "First find the total number of cans Jack can fit on one shelf: {cans_per_row} cans/row * {rows_per_shelf} rows/shelf = {cans_per_shelf} cans/shelf"
    },
    {
      "line_number": "L2",
      "question_inputs": ["shelves_per_closet"],
      "WK_inputs": [],
      "output_variable": "total_cans_in_closet",
      "solution_line_template": "Then find the number of cans he can fit on all the shelves in one closet: {cans_per_shelf} cans/shelf * {shelves_per_closet} shelves = <<{cans_per_shelf}*{shelves_per_closet}={total_cans_in_closet}>>{total_cans_in_closet} cans"
    }
  ]
}
```