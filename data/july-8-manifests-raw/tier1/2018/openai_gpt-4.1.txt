{
  "function_code": "def solve():\n    \"\"\"Index: 2018.\n    Returns: Jessica's total monthly payment if she adds movie and sports channels.\n    \"\"\"\n    # L1\n    movie_channels_cost = 12 # movie channels, it will cost an additional $12 per month\n    sports_channels_less = 3 # sports channels cost $3 less per month than the movie channels\n    sports_channels_cost = movie_channels_cost - sports_channels_less\n\n    # L2\n    basic_cable_cost = 15 # basic cable television service for $15 per month\n    total_monthly_payment = basic_cable_cost + movie_channels_cost + sports_channels_cost\n\n    # FA\n    answer = total_monthly_payment\n    return answer",
  "logical_steps": [
    {
      "line_number": "L1",
      "question_inputs": ["movie_channels_cost", "sports_channels_less"],
      "WK_inputs": [],
      "output_variable": "sports_channels_cost",
      "solution_line_template": "The sports channels cost ${movie_channels_cost} - ${sports_channels_less} = $<<{movie_channels_cost}-{sports_channels_less}={sports_channels_cost}>>{sports_channels_cost} per month."
    },
    {
      "line_number": "L2",
      "question_inputs": ["basic_cable_cost"],
      "WK_inputs": [],
      "output_variable": "total_monthly_payment",
      "solution_line_template": "Jessica has to pay ${basic_cable_cost} + ${movie_channels_cost} + ${sports_channels_cost} = $<<{basic_cable_cost}+{movie_channels_cost}+{sports_channels_cost}={total_monthly_payment}>>{total_monthly_payment} in total per month."
    }
  ]
}