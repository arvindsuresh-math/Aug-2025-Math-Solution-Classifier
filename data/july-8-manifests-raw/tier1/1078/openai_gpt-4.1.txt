{
  "function_code": "def solve():\n    \"\"\"Index: 1078.\n    Returns: the total number of fish at Glenda's wedding reception.\n    \"\"\"\n    # L1\n    num_tables = 32 # There are 32 tables\n    fish_per_table = 2 # 2 fish in a bowl on each\n    fish_regular_tables = num_tables * fish_per_table\n\n    # L2\n    extra_fish = 1 # one table has an additional fish\n    total_fish = fish_regular_tables + extra_fish\n\n    # FA\n    answer = total_fish\n    return answer",
  "logical_steps": [
    {
      "line_number": "L1",
      "question_inputs": ["num_tables", "fish_per_table"],
      "WK_inputs": [],
      "output_variable": "fish_regular_tables",
      "solution_line_template": "There are {num_tables} tables with {fish_per_table} fish in a bowl on each, so {num_tables} tables x {fish_per_table} fish = <<{num_tables}*{fish_per_table}={fish_regular_tables}>>{fish_regular_tables} fish."
    },
    {
      "line_number": "L2",
      "question_inputs": ["extra_fish"],
      "WK_inputs": [],
      "output_variable": "total_fish",
      "solution_line_template": "One table has an additional fish, so {fish_regular_tables} fish + {extra_fish}xtra fish = {total_fish} fish."
    }
  ]
}