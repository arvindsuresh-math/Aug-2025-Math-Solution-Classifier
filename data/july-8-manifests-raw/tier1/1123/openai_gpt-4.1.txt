{
  "function_code": "def solve():\n    \"\"\"Index: 1123.\n    Returns: the amount Evan still needs to buy the watch.\n    \"\"\"\n    # L1\n    evan_initial = 1 # Evan who has $1\n    david_found = 12 # David found $12\n    evan_total = evan_initial + david_found\n\n    # L2\n    watch_price = 20 # watch worth $20\n    evan_still_needs = watch_price - evan_total\n\n    # FA\n    answer = evan_still_needs\n    return answer",
  "logical_steps": [
    {
      "line_number": "L1",
      "question_inputs": ["evan_initial", "david_found"],
      "WK_inputs": [],
      "output_variable": "evan_total",
      "solution_line_template": "The total amount Evan has is ${evan_initial} + ${david_found} = $<<{evan_initial}+{david_found}={evan_total}>>{evan_total}."
    },
    {
      "line_number": "L2",
      "question_inputs": ["watch_price"],
      "WK_inputs": [],
      "output_variable": "evan_still_needs",
      "solution_line_template": "So Evan still needs ${watch_price} - ${evan_total} = $<<{watch_price}-{evan_total}={evan_still_needs}>>{evan_still_needs}."
    }
  ]
}