{
  "function_code": "from fractions import Fraction\n\ndef solve():\n    \"\"\"Index: 3592.\n    Returns: the time it takes for Tom to finish the program.\n    \"\"\"\n    # L1\n    bs_years = 3 # 3 years to finish the BS\n    phd_years = 5 # 5 years to finish the Ph.D\n    normal_duration = bs_years + phd_years\n\n    # L2\n    time_fraction = Fraction(3, 4) # 3/4ths the time\n    actual_duration = normal_duration * time_fraction\n\n    # FA\n    answer = actual_duration\n    return answer",
  "logical_steps": [
    {
      "line_number": "L1",
      "question_inputs": ["bs_years", "phd_years"],
      "WK_inputs": [],
      "output_variable": "normal_duration",
      "solution_line_template": "The program normally takes {bs_years}+{phd_years}=<<{bs_years}+{phd_years}={normal_duration}>>{normal_duration} years"
    },
    {
      "line_number": "L2",
      "question_inputs": ["time_fraction"],
      "WK_inputs": [],
      "output_variable": "actual_duration",
      "solution_line_template": "So he manages to finish in {normal_duration}*{time_fraction}=<<{normal_duration}*{time_fraction}={actual_duration}>>{actual_duration} years"
    }
  ]
}